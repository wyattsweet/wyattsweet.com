{
  "name": "cra_portfolio",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^16.2.0",
    "react-dom": "^16.2.0",
    "react-router-dom": "^4.2.2",
    "react-scripts": "1.0.17"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "deploy": "npm run build && cd build && mv index.html 200.html && surge . --domain wyattsweet.com && cd ..",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
